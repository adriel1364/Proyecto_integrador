// Saludo.jsx
import React from 'react';

// Componente que muestra un saludo personalizado
const Saludo = ({ nombre }) => {
    return (
        <h1>Hola {nombre}</h1>
    );
};

export default Saludo;
